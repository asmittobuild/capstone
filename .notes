internal notes, NOT FOR AI agent

The workflow chain is now:

/speckit.constitution — establish principles
/speckit.specify — define requirements
/speckit.plan — technical plan
/speckit.tasks — task breakdown
/speckit.taskstoissues — push to GitHub Issues (built-in)
/speckit.implementfromissues — iterative issue-driven implementation (custom)


used custom spec kit
i used clarify several times even until there were no clarifications pending

then speckit plan

decided to switch to hosted db; it tried to convince me to stick to local othrewise it was a major architecture change and needed a major amendement to the constitution; which I found interesting and actually liked it; since it AIs usually don't argue with you :p

azure cosmos db asmitto-ai-bootcamp

during planning it went overboard creating a new backenb server; so I had to guide it to go back to running a "SPA". I was surprised for such a leap cause up until that point it really understood the point of the app being 'minimalistic'



installed the mcp

asked it to sync up the changes /create repo (it used the mcp server for that)


before implementation I decided to check how to re do the spec for the stable difussion implementation with specify
plan, tasks

then analyze for consistency

then proceeded to task to issues

used subagents to create them in batches (didn't do it in parallel)

implement from issues

it figured out structure changes while implementing

app didn't work otb

the app worked pretty much out of the box after configuring the right model on HF

I ran out of sonnet at this point and got forced to gpt

the major gap I see is that in many cases things we would assume would be clarified are not (how to generate the image for example)
I let it run and then I worked on an update to see how the flow would be. but when I did; I thought it would basically update the tasks needed to do the new thing from scratch; but it instead updated them based on the existing code; so it assumed there was code already present; so the end result was an inconssitent spec 99% creating new code but with a task to update code that (if ran from scratch) wouldn't exist
So i pushed on that; and it basically said 'oh yeah i should've done a patch (which is what I expected)

after this it worked fine but it got really slow (i assumebecause of gpt4.1) and I also noticed it got dumber (a lot) so I decided to polish it vibe coding as I didn't really trust gpt4.1 and it was so slow that it made it tedious to go back and forth

